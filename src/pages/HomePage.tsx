import React from 'react';
import { SearchInput, ShowsList } from '../components';

const HomePage = () => {
    return (
        <div>
            <SearchInput />
            <ShowsList />
        </div>
    );
};

export default HomePage;